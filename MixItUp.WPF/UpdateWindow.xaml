<Windows:LoadingWindowBase
    x:Class="MixItUp.WPF.UpdateWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MixItUp.WPF.Controls"
    xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:Windows="clr-namespace:MixItUp.WPF.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MixItUp.WPF"
    xmlns:markdown="clr-namespace:MdXaml;assembly=MdXaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:MixItUp.Base;assembly=MixItUp.Base"
    Title="{x:Static resx:Resources.MixItUpUpdateAvailable}"
    Width="969"
    Height="700"
    AllowsTransparency="True"
    Background="Transparent"
    FontFamily="{StaticResource NotoSans}"
    ResizeMode="NoResize"
    TextElement.FontSize="14"
    TextElement.FontWeight="Medium"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <MaterialDesign:DialogHost x:Name="MDDialogHost" Identifier="RootDialog">
            <MaterialDesign:Card
                Padding="0"
                MaterialDesign:ElevationAssist.Elevation="Dp16"
                Background="{DynamicResource MaterialDesign.Brush.Background}"
                Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                UniformCornerRadius="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!--  Two-column body: Patreon CTA (left) + Update content (right)  -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="240" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!--  Left: Patreon CTA  -->
                        <Border
                            Grid.Column="0"
                            BorderBrush="{DynamicResource MaterialDesignDivider}"
                            BorderThickness="0,0,1,0">
                            <StackPanel
                                Margin="24,30,24,30"
                                VerticalAlignment="Center">

                                <!--  Patreon header  -->
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    Margin="0,0,0,20"
                                    Orientation="Horizontal">
                                    <Image
                                        Width="24"
                                        Height="24"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center"
                                        Source="/Assets/Images/patreon-color_sm.png" />
                                    <TextBlock
                                        FontSize="17"
                                        FontWeight="SemiBold"
                                        Text="Patreon"
                                        VerticalAlignment="Center" />
                                </StackPanel>

                                <!--  Member shoutout — shown when a member is fetched  -->
                                <Border
                                    x:Name="PatreonShoutoutBorder"
                                    Margin="0,0,0,16"
                                    Padding="12,12"
                                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Visibility="Collapsed">
                                    <StackPanel>
                                        <TextBlock
                                            x:Name="PatreonLeadInTextBlock"
                                            FontSize="11"
                                            Margin="0,0,0,10"
                                            Opacity="0.7"
                                            TextAlignment="Center"
                                            TextWrapping="Wrap" />
                                        <Border
                                            Width="64"
                                            Height="64"
                                            Margin="0"
                                            HorizontalAlignment="Center"
                                            BorderBrush="{DynamicResource MaterialDesign.Brush.Primary}"
                                            BorderThickness="4"
                                            CornerRadius="32">
                                            <Image
                                                Width="56"
                                                Height="56" 
                                                x:Name="PatreonMemberAvatarImage"
                                                Stretch="UniformToFill" >
                                                <Image.Clip>
                                                    <EllipseGeometry Center="28,28" RadiusX="28" RadiusY="28" />
                                                </Image.Clip>
                                            </Image>
                                        </Border>
                                        <TextBlock
                                            x:Name="PatreonMemberNameTextBlock"
                                            FontSize="13"
                                            FontWeight="SemiBold"
                                            TextAlignment="Center"
                                            TextWrapping="Wrap" />
                                        <Button
                                            x:Name="PatreonSocialButton"
                                            Margin="0,8,0,0"
                                            HorizontalAlignment="Center"
                                            Click="PatreonSocialButton_Click"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"
                                            Padding="8,4"
                                            Visibility="Collapsed">
                                            <StackPanel Orientation="Horizontal">
                                                <Image
                                                    x:Name="PatreonSocialPlatformImage"
                                                    Width="16"
                                                    Height="16"
                                                    Margin="0,0,6,0"
                                                    VerticalAlignment="Center" />
                                                <TextBlock
                                                    x:Name="PatreonSocialUsernameTextBlock"
                                                    FontSize="11"
                                                    VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Border>

                                <Separator Margin="0,0,0,16" />

                                <!--  CTA body  -->
                                <TextBlock
                                    x:Name="PatreonCommunityTextBlock"
                                    FontSize="12"
                                    LineHeight="18"
                                    Margin="0,0,0,12"
                                    Opacity="0.85"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                                <TextBlock
                                    x:Name="PatreonCTATextBlock"
                                    FontSize="12"
                                    LineHeight="18"
                                    Margin="0,0,0,20"
                                    Opacity="0.85"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />

                                <Button
                                    HorizontalAlignment="Center"
                                    Click="PatreonButton_Click"
                                    Style="{StaticResource MaterialDesignRaisedButton}">
                                    Support on Patreon
                                </Button>

                            </StackPanel>
                        </Border>

                        <!--  Right: Update content  -->
                        <Grid
                            Grid.Column="1"
                            Margin="30,30,30,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Button
                                Grid.Row="0"
                                Width="32"
                                Height="32"
                                Margin="0,-10,-10,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Click="CloseButton_Click"
                                Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="{x:Static resx:Resources.Close}">
                                <Controls:MaterialSymbolIcon
                                    IconName="close" FontSize="20" />
                            </Button>

                            <Grid
                                x:Name="MandatoryBanner"
                                Grid.Row="1"
                                MaxWidth="600"
                                Margin="0,0,0,15"
                                HorizontalAlignment="Center"
                                Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Controls:MaterialSymbolIcon
                                    Grid.Column="0"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource MaterialDesign.Brush.Primary}"
                                    IconName="error" FontSize="20" />
                                <TextBlock
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    FontWeight="SemiBold"
                                    Text="{x:Static resx:Resources.ThisUpdateIsRequired}"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                            </Grid>

                            <Grid
                                Grid.Row="2"
                                MaxWidth="600"
                                Margin="0,0,0,15"
                                HorizontalAlignment="Center">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="22"
                                    FontWeight="SemiBold"
                                    Text="{x:Static resx:Resources.ANewUpdateIsAvailableForMixItUp}"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                            </Grid>

                            <WrapPanel
                                Grid.Row="3"
                                Margin="0,0,0,15"
                                HorizontalAlignment="Center"
                                Orientation="Horizontal">
                                <Border
                                    Padding="12,6"
                                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                                    BorderThickness="1"
                                    CornerRadius="6">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            Opacity="0.7"
                                            Text="{Binding Source={x:Static resx:Resources.CurrentVersion}, StringFormat={x:Static resx:Resources.ColumnHeaderFormat}}" />
                                        <TextBlock
                                            x:Name="CurrentVersionTextBlock"
                                            Margin="4,0,0,0"
                                            VerticalAlignment="Center"
                                            FontWeight="SemiBold" />
                                    </StackPanel>
                                </Border>
                                <Controls:MaterialSymbolIcon
                                    Margin="12,0"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource MaterialDesign.Brush.Primary}"
                                    IconName="arrow_forward" FontSize="24" />
                                <Border
                                    Padding="12,6"
                                    Background="{DynamicResource MaterialDesign.Brush.Primary}"
                                    CornerRadius="6">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            Foreground="White"
                                            Text="{Binding Source={x:Static resx:Resources.NewVersion}, StringFormat={x:Static resx:Resources.ColumnHeaderFormat}}" />
                                        <TextBlock
                                            x:Name="NewVersionTextBlock"
                                            Margin="4,0,0,0"
                                            VerticalAlignment="Center"
                                            FontWeight="SemiBold"
                                            Foreground="White" />
                                    </StackPanel>
                                </Border>
                            </WrapPanel>

                            <Border
                                x:Name="PreviewUpdateGrid"
                                Grid.Row="4"
                                Margin="0,0,0,10"
                                Padding="12,8"
                                BorderBrush="{DynamicResource MaterialDesignDivider}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Visibility="Collapsed">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Controls:MaterialSymbolIcon
                                            Grid.Column="0"
                                            Margin="0,0,8,0"
                                            VerticalAlignment="Top"
                                            Foreground="{DynamicResource MaterialDesign.Brush.Primary}"
                                            IconName="warning" FontSize="20" />
                                        <TextBlock
                                            Grid.Column="1"
                                            Text="{x:Static resx:Resources.PreviewUpdateWarning1}"
                                            TextWrapping="Wrap" />
                                    </Grid>
                                    <TextBlock
                                        Margin="28,6,0,0"
                                        Text="{x:Static resx:Resources.PreviewUpdateWarning2}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>

                            <Border
                                Grid.Row="6"
                                Margin="5,0"
                                Padding="10"
                                BorderBrush="{DynamicResource MaterialDesignDivider}"
                                BorderThickness="1"
                                CornerRadius="8">
                                <markdown:MarkdownScrollViewer x:Name="UpdateChangelogViewer" />
                            </Border>

                            <StackPanel
                                Grid.Row="7"
                                Margin="0,20,0,0"
                                HorizontalAlignment="Center"
                                Orientation="Horizontal">
                                <Button
                                    x:Name="DownloadUpdateButton"
                                    Height="40"
                                    MinWidth="130"
                                    Click="DownloadUpdateButton_Click"
                                    Content="{x:Static resx:Resources.Update}"
                                    Style="{StaticResource MaterialDesignRaisedButton}" />
                                <Button
                                    x:Name="SkipUpdateButton"
                                    Height="40"
                                    MinWidth="100"
                                    Margin="20,0,0,0"
                                    Click="SkipUpdateButton_Click"
                                    Content="{x:Static resx:Resources.Skip}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Visibility="Collapsed" />
                            </StackPanel>

                            <TextBlock
                                Grid.Row="8"
                                Margin="0,15,0,15"
                                FontSize="12"
                                Opacity="0.6"
                                Text="{x:Static resx:Resources.IfUpdateDoesNotCompleteRestartComputer}"
                                TextAlignment="Center"
                                TextWrapping="Wrap" />

                        </Grid>
                    </Grid>

                    <!--  StatusBar spans full card width  -->
                    <Controls:LoadingStatusBar
                        x:Name="StatusBar"
                        Grid.Row="1" />
                </Grid>
            </MaterialDesign:Card>
        </MaterialDesign:DialogHost>
    </Grid>
</Windows:LoadingWindowBase>
