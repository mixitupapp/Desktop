<Controls:LoadingControlBase x:Class="MixItUp.WPF.Controls.Overlay.OverlayWheelV3Control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MixItUp.WPF.Controls.Overlay"
             xmlns:Util="clr-namespace:MixItUp.WPF.Util"
             xmlns:resx="clr-namespace:MixItUp.Base;assembly=MixItUp.Base"
             xmlns:Controls="clr-namespace:MixItUp.WPF.Controls"
             xmlns:CommandControls="clr-namespace:MixItUp.WPF.Controls.Commands"
             xmlns:OverlayControls="clr-namespace:MixItUp.WPF.Controls.Overlay"
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Controls:LoadingControlBase.Resources>
        <Util:LocalizationConverter x:Key="LocalizationConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Controls:LoadingControlBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="125" />
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding FontSize}" Grid.Column="0" MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.FontSize}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" AutomationProperties.AutomationId="OverlayWheelV3.FontSizeTextBox" AutomationProperties.Name="{x:Static resx:Resources.FontSize}" />

            <ComboBox Text="{Binding FontName}" Grid.Column="2" ItemsSource="{Binding Fonts}" IsEditable="True" MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.FontName}" Style="{StaticResource MaterialDesignFloatingHintComboBox}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.ScrollUnit="Pixel" VirtualizingPanel.VirtualizationMode="Recycling" AutomationProperties.AutomationId="OverlayWheelV3.FontNameComboBox" AutomationProperties.Name="{x:Static resx:Resources.FontName}">
                <ComboBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </ComboBox.ItemsPanel>
            </ComboBox>

            <Controls:ColorPickerTextBox ColorText="{Binding FontColor}" Grid.Column="4" HintText="{x:Static resx:Resources.FontColor}" AutomationProperties.AutomationId="OverlayWheelV3.FontColorColorPicker" AutomationProperties.Name="{x:Static resx:Resources.FontColor}" />

            <TextBox Text="{Binding Size}" Grid.Column="6" MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.Size}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" AutomationProperties.AutomationId="OverlayWheelV3.SizeTextBox" AutomationProperties.Name="{x:Static resx:Resources.Size}" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding WheelClickSoundFilePath}" Grid.Column="0" VerticalAlignment="Center" MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.OverlayWheelClickSoundFilePath}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" AutomationProperties.AutomationId="OverlayWheelV3.OverlayWheelClickSoundFilePathTextBox" AutomationProperties.Name="{x:Static resx:Resources.OverlayWheelClickSoundFilePath}" />

            <Button Grid.Column="2" Command="{Binding BrowseFilePathCommand}" Content="{x:Static resx:Resources.Browse}" VerticalAlignment="Center" AutomationProperties.AutomationId="OverlayWheelV3.BrowseButton" />

            <TextBlock Grid.Column="4" VerticalAlignment="Center" Text="{x:Static resx:Resources.VolumeHeader}"/>
            <Slider Value="{Binding WheelClickVolume}" VerticalAlignment="Center" Grid.Column="6" Width="120" Minimum="0" Maximum="100" Style="{StaticResource MaterialDesign3.MaterialDesignDiscreteSlider}" ToolTip="{x:Static resx:Resources.Volume}" AutomationProperties.AutomationId="OverlayWheelV3.VolumeSlider" AutomationProperties.Name="{x:Static resx:Resources.Volume}" />

            <CommandControls:CustomCommandListingControl Grid.Column="8" DataContext="{Binding DefaultOutcomeCommand}" Header="{x:Static resx:Resources.DefaultOutcome}" />
        </Grid>

        <GroupBox Grid.Row="4" Header="{x:Static resx:Resources.Outcomes}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="5" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{Binding Source={x:Static resx:Resources.EqualProbabilityForOutcomes}, StringFormat={x:Static resx:Resources.ColumnHeaderFormat}}" />
                    <CheckBox Grid.Column="2" IsChecked="{Binding EqualProbabilityForOutcomes}" VerticalAlignment="Center" Margin="5,0,0,0" AutomationProperties.AutomationId="OverlayWheelV3.EqualProbabilityForOutcomesCheckBox" AutomationProperties.Name="{x:Static resx:Resources.EqualProbabilityForOutcomes}" />

                    <Button Grid.Column="4" Command="{Binding AddOutcomeCommand}" Content="{x:Static resx:Resources.Add}" Width="100" AutomationProperties.AutomationId="OverlayWheelV3.AddButton" />
                </Grid>

                <ItemsControl Grid.Row="2" ItemsSource="{Binding Outcomes}" AutomationProperties.AutomationId="OverlayWheelV3.OutcomesList" AutomationProperties.Name="{x:Static resx:Resources.Prizes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="15" />
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="15" />
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="15" />
                                    <ColumnDefinition Width="125" />
                                    <ColumnDefinition Width="15" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="10" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="5" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="5" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0" Text="{Binding Name}" MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.Name}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" AutomationProperties.AutomationId="WheelOutcome.NameTextBox" AutomationProperties.Name="{x:Static resx:Resources.Name}" />

                                <Grid Grid.Column="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBox Grid.Column="0" Text="{Binding Probability}" IsEnabled="{Binding CanSetProbabilities}" MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.Probability}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" AutomationProperties.AutomationId="WheelOutcome.ProbabilityTextBox" AutomationProperties.Name="{x:Static resx:Resources.Probability}" />
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="0,10,0,0">%</TextBlock>
                                </Grid>

                                <TextBox Grid.Column="4" Text="{Binding Modifier}" IsEnabled="{Binding CanSetProbabilities}" MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.Modifier}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" AutomationProperties.AutomationId="WheelOutcome.ModifierTextBox" AutomationProperties.Name="{x:Static resx:Resources.Modifier}" />

                                <Controls:ColorPickerTextBox Grid.Column="6" ColorText="{Binding Color}" HintText="{x:Static resx:Resources.Color}" AutomationProperties.AutomationId="WheelOutcome.ColorColorPicker" AutomationProperties.Name="{x:Static resx:Resources.Color}" />

                                <CommandControls:CommandListingButtonsControl Grid.Column="8" EditClicked="CommandListingButtonsControl_EditClicked" HideDeleteButton="True" HideEnableDisableToggle="True" />

                                <Controls:IconButton Grid.Column="10" Command="{Binding MoveUpCommand}" HorizontalAlignment="Center" Icon="arrow_upward" ToolTip="{x:Static resx:Resources.MoveUp}" AutomationProperties.AutomationId="WheelOutcome.MoveUpButton" AutomationProperties.Name="{Binding Name, StringFormat={x:Static resx:Resources.MoveUpFormat}}" />

                                <Controls:IconButton Grid.Column="12" Command="{Binding MoveDownCommand}" HorizontalAlignment="Center" Icon="arrow_downward" ToolTip="{x:Static resx:Resources.MoveDown}" AutomationProperties.AutomationId="WheelOutcome.MoveDownButton" AutomationProperties.Name="{Binding Name, StringFormat={x:Static resx:Resources.MoveDownFormat}}" />

                                <Controls:IconButton Grid.Column="14" Command="{Binding DeleteCommand}" HorizontalAlignment="Center" Icon="delete" ToolTip="{x:Static resx:Resources.Delete}" AutomationProperties.AutomationId="WheelOutcome.DeleteButton" AutomationProperties.Name="{Binding Name, StringFormat={x:Static resx:Resources.DeleteCommandFormat}}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Rectangle Grid.Row="4" Fill="{DynamicResource MaterialDesign.Brush.Primary}" Margin="0,-2" />

                <Grid Grid.Row="6" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="90" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="290" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{x:Static resx:Resources.Total}" FontWeight="Bold" />

                    <TextBlock Grid.Column="2" Text="{Binding TotalProbability}" FontWeight="Bold" HorizontalAlignment="Center" />

                    <TextBlock Grid.Column="4" Text="{Binding TotalModifier}" FontWeight="Bold" HorizontalAlignment="Center" />
                </Grid>
            </Grid>
        </GroupBox>
    </Grid>
</Controls:LoadingControlBase>
