<local:ServiceControlBase
    x:Class="MixItUp.WPF.Controls.Services.GoogleCloudTTSServiceControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MixItUp.WPF.Controls.Services"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:MixItUp.Base;assembly=MixItUp.Base"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <local:ServiceControlBase.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </local:ServiceControlBase.Resources>

    <Grid Margin="10" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="450" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                VerticalAlignment="Center"
                Text="{x:Static resx:Resources.GoogleCloudTTSInfo1}"
                TextWrapping="Wrap" />

            <TextBlock
                Grid.Row="2"
                VerticalAlignment="Center"
                Text="{x:Static resx:Resources.GoogleCloudTTSInfo2}"
                TextWrapping="Wrap" />

            <TextBlock Grid.Row="4" TextWrapping="Wrap">
                <Hyperlink NavigateUri="https://wiki.mixitupapp.com/en/services/google-cloud-tts" RequestNavigate="Hyperlink_RequestNavigate">
                    <TextBlock Text="{x:Static resx:Resources.GoogleCloudTTS}" />
                </Hyperlink>
            </TextBlock>
        </Grid>

        <Grid
            Grid.Column="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Grid Visibility="{Binding IsConnected, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button
                    MinWidth="90"
                    HorizontalAlignment="Center"
                    Command="{Binding LogOutCommand}"
                    Content="{x:Static resx:Resources.Disconnect}" />
            </Grid>

            <Grid Visibility="{Binding IsNotConnected, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBox
                    Grid.Row="0"
                    Width="250"
                    MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.APIKey}"
                    Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                    Text="{Binding APIKey}" />

                <Button
                    Grid.Row="2"
                    MinWidth="90"
                    HorizontalAlignment="Center"
                    Command="{Binding LogInCommand}"
                    Content="{x:Static resx:Resources.Connect}" />
            </Grid>
        </Grid>
    </Grid>
</local:ServiceControlBase>