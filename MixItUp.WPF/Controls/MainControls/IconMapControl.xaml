<local:MainControlBase x:Class="MixItUp.WPF.Controls.MainControls.IconMapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MixItUp.WPF.Controls.MainControls"
             xmlns:Controls="clr-namespace:MixItUp.WPF.Controls"
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="6,0,6,10" TextWrapping="Wrap"
                   Text="PackIcon (mdi) → Material Symbols Sharp migration preview. Proposed replacement is shown next to the arrow; dimmed candidates to the right are alternates." />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="MapItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="6,8" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220" />
                                    <ColumnDefinition Width="40" />
                                    <ColumnDefinition Width="40" />
                                    <ColumnDefinition Width="220" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="300" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{Binding KindName}" />

                                <MaterialDesign:PackIcon Grid.Column="1" Width="24" Height="24" VerticalAlignment="Center" Kind="{Binding Kind}" />

                                <Controls:MaterialSymbolIcon Grid.Column="2" VerticalAlignment="Center" FontSize="20" Opacity="0.4" IconName="arrow_right_alt" />

                                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Controls:MaterialSymbolIcon FontSize="24" VerticalAlignment="Center" IconName="{Binding Primary}" />
                                    <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" FontSize="11" Opacity="0.75" Text="{Binding Primary}" />
                                </StackPanel>

                                <ItemsControl Grid.Column="4" VerticalAlignment="Center" Opacity="0.55" ItemsSource="{Binding Alternates}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="0,0,20,0" Orientation="Horizontal">
                                                <Controls:MaterialSymbolIcon FontSize="24" VerticalAlignment="Center" IconName="{Binding}" />
                                                <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" FontSize="11" Text="{Binding}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <TextBlock Grid.Column="5" VerticalAlignment="Center" FontSize="11" Opacity="0.6" TextWrapping="Wrap" Text="{Binding Usage}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</local:MainControlBase>
