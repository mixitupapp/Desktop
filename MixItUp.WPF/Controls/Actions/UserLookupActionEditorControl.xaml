<local:ActionEditorControlBase
    x:Class="MixItUp.WPF.Controls.Actions.UserLookupActionEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:System="clr-namespace:System;assembly=System.Runtime"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MixItUp.WPF.Controls.Actions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:MixItUp.Base;assembly=MixItUp.Base"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                Text="{Binding Source={x:Static resx:Resources.Platform}, StringFormat={x:Static resx:Resources.ColumnHeaderFormat}}" />
            <ComboBox
                Grid.Column="2"
                Width="150"
                VerticalAlignment="Center"
                ItemsSource="{Binding PlatformOptions}"
                SelectedItem="{Binding SelectedPlatform, Mode=TwoWay}"
                AutomationProperties.AutomationId="UserLookupActionEditor.PlatformComboBox"
                AutomationProperties.Name="{x:Static resx:Resources.Platform}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image
                                Width="20"
                                Height="20"
                                VerticalAlignment="Center"
                                Source="{Binding LogoPath, TargetNullValue={x:Null}}" />
                            <TextBlock
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Text="{Binding Name}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock
                Grid.Column="3"
                HorizontalAlignment="Right"
                VerticalAlignment="Center">
                <Hyperlink NavigateUri="https://wiki.mixitup.bot/special-identifiers" RequestNavigate="Hyperlink_RequestNavigate" AutomationProperties.AutomationId="UserLookupActionEditor.SpecialIdentifiersLink">
                    <TextBlock Text="{x:Static resx:Resources.SpecialIdentifiers}" />
                </Hyperlink>
            </TextBlock>
        </Grid>

        <TextBox
            Grid.Row="2"
            MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.UserLookupUsernameOrID}"
            Style="{StaticResource MaterialDesignOutlinedTextBox}"
            Text="{Binding UsernameOrID}"
            TextAlignment="Left"
            AutomationProperties.AutomationId="UserLookupActionEditor.UsernameOrIDTextBox"
            AutomationProperties.Name="{x:Static resx:Resources.UserLookupUsernameOrID}" />

    </Grid>
</local:ActionEditorControlBase>
