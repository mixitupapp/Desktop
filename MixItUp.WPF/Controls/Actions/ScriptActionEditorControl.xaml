<local:ActionEditorControlBase x:Class="MixItUp.WPF.Controls.Actions.ScriptActionEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:resx="clr-namespace:MixItUp.Base;assembly=MixItUp.Base"
             xmlns:System="clr-namespace:System;assembly=System.Runtime"
             xmlns:local="clr-namespace:MixItUp.WPF.Controls.Actions"
             xmlns:Util="clr-namespace:MixItUp.WPF.Util"
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <local:ActionEditorControlBase.Resources>
        <Util:LocalizationConverter x:Key="LocalizationConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </local:ActionEditorControlBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="ScriptActionEditorActionTypeComboBox" Grid.Column="0" ItemsSource="{Binding ActionTypes}" SelectedItem="{Binding SelectedActionType}" MaterialDesign:HintAssist.Hint="{x:Static resx:Resources.Type}" Style="{StaticResource MaterialDesignFloatingHintComboBox}" SelectionChanged="ActionTypeComboBox_SelectionChanged" AutomationProperties.AutomationId="ScriptActionEditor.ActionTypeComboBox" AutomationProperties.Name="{x:Static resx:Resources.Type}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource LocalizationConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Grid Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{Binding Source={x:Static resx:Resources.Result}, StringFormat={x:Static resx:Resources.ColumnHeaderFormat}}" />
                <TextBox Grid.Column="2" VerticalAlignment="Center" Text="$scriptresult" Style="{StaticResource TextBoxReadOnlySelectable}" AutomationProperties.AutomationId="ScriptActionEditor.ResultTextBox" AutomationProperties.Name="{x:Static resx:Resources.Result}" />
                <TextBlock Grid.Column="4" VerticalAlignment="Center">
                    <Hyperlink Click="LogsHyperlink_Click" AutomationProperties.AutomationId="ScriptActionEditor.ResultLink">Open Logs Folder</Hyperlink>
                </TextBlock>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource DefaultBorder}">
            <avalonEdit:TextEditor
                x:Name="ScriptEditor"
                Util:AvalonEditBehaviour.Text="{Binding Script}"
                ShowLineNumbers="True"
                FontFamily="Consolas"
                MinHeight="300"
                MinWidth="700"
                Background="{DynamicResource MaterialDesignPaper}"
                Foreground="{DynamicResource MaterialDesignBody}"
                AutomationProperties.AutomationId="ScriptActionEditor.ScriptEditor"
                AutomationProperties.Name="{x:Static resx:Resources.Script}" />
        </Border>
    </Grid>
</local:ActionEditorControlBase>
